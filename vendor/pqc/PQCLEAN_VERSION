# Veld vendored PQClean provenance identity.
#
# The exhaustive raw-byte inventory is:
#   vendor/pqc/provenance/PQC_PROVENANCE.tsv
# Verify it offline before every compile/package operation with:
#   python3 scripts/verify-pqc-provenance.py
# Supply an exact upstream checkout for byte identity and patch reconstruction:
#   python3 scripts/verify-pqc-provenance.py --upstream-dir /path/to/PQClean

PQCLEAN_UPSTREAM_REPO=https://github.com/PQClean/PQClean
PQCLEAN_UPSTREAM_COMMIT=202a8f96315f9ed219387a50f7e40d04af037ea8
PQCLEAN_VENDORED_DATE=2026-05-08
PQCLEAN_PROVENANCE_FORMAT=VELD_PQC_RAW_BYTES_V1

# Local changes are exact checked-in patches, not prose-only exceptions.
PQCLEAN_PATCH=vendor/pqc/provenance/vendor-patches/0001-mldsa65-sign-constant-time.patch
PQCLEAN_PATCH=vendor/pqc/provenance/vendor-patches/0002-randombytes-fail-closed-webcrypto.patch
PQCLEAN_PATCH=vendor/pqc/provenance/vendor-patches/0003-mldsa65-veld-seedgen.patch

# The distributed JavaScript/WASM payload is canonical at
# vendor/pqc/dilithium_wasm.js and deterministically embedded in
# include/network/dilithium_wasm_js.h. A public desktop/GUI build additionally
# requires a current PASS in wasm-rebuild-attestation.tsv, bound to the exact
# input digest, generated bytes, embedded-header bytes, and runtime smoke.

# The compiled startup KAT is generated from the same exact revision by:
#   scripts/generate-mldsa65-nist-kat.py
# Its canonical first-response SHA-256 is recorded in the manifest and header;
# no deterministic seed or secret-key field is retained in the repository.
